-- module should be on our runtime path (rtp)
-- search :help rtp
--
-- convention is to use same name as file
-- that returns the module to
-- store it
local module = require("module")

module.cool_function()
